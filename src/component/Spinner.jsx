import spinnerImage from "/images/error/spinner.gif"

const Spinner = () => {
    return (
        <div>
            <img src={spinnerImage} alt="spinner" />
        </div>
    );
};

export default Spinner;